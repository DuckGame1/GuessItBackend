stages:
  - build
  - docker

variables:
  BUILD_CONFIGURATION: "Release"
  IMAGE_NAME: "guessitapi"

build:
  stage: build
  image: mcr.microsoft.com/dotnet/sdk:8.0
  script:
    - dotnet restore
    - dotnet build -c $BUILD_CONFIGURATION
  artifacts:
    paths:
      - bin/
      - obj/

docker:
  stage: docker
  image: mcr.microsoft.com/dotnet/sdk:8.0  # можно любой образ с docker-cli, но .NET SDK тоже ок
  variables:
    DOCKER_HOST: unix:///var/run/docker.sock
  script:
    - docker build -t $IMAGE_NAME:$CI_COMMIT_SHORT_SHA .
    - docker stop guessitapi || true
    - docker rm guessitapi || true
    - docker run -d \
        --name guessitapi \
        --network proxy \
        -e VIRTUAL_HOST=guessitapi.antishelkon.ru \
        -e LETSENCRYPT_HOST=guessitapi.antishelkon.ru \
        $IMAGE_NAME:$CI_COMMIT_SHORT_SHA
